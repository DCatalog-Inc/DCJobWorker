#
obj/
bin/
Tools/
.vs/
secrets/
publish/
# deploy staging artifacts (recursive, do not commit)
bundle-*/
publish-*/
.rbdepid

# keep the patched PDFUtils.exe (page-insert fix) tracked so every machine has it
!Tools/PDFUtils/PDFUtils.exe
